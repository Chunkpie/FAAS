kind = "web"
previewPath = "/"
title = "Freight Audit Automation System"
version = "1.0.0"
id = "artifacts/faas"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25116

[services.development]
run = "pnpm --filter @workspace/faas run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/faas", "run", "build" ]
publicDir = "artifacts/faas/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25116"
BASE_PATH = "/"
